{
  "name": "verai-cursor",
  "displayName": "Verkeep Verify Session Usage",
  "description": "See which models this Cursor session mostly used (local-only; unified call mix).",
  "version": "0.1.0",
  "publisher": "verai",
  "license": "MIT",
  "icon": "media/app-icon-128.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/black-pupil153/VerAI.git",
    "directory": "extensions/verai-cursor"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:verai.sessionView",
    "onCommand:verai.showSessionUsage",
    "onCommand:verai.refresh",
    "onCommand:verai.openLatest"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "verai",
          "title": "Verkeep Verify",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "verai": [
        {
          "type": "webview",
          "id": "verai.sessionView",
          "name": "Session Usage"
        }
      ]
    },
    "commands": [
      {
        "command": "verai.showSessionUsage",
        "title": "Verkeep Verify: Show Session Usage",
        "icon": "$(graph)"
      },
      {
        "command": "verai.refresh",
        "title": "Verkeep Verify: Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "verai.openLatest",
        "title": "Verkeep Verify: Open Latest Session"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "verai.refresh",
          "when": "view == verai.sessionView",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Verkeep Verify",
      "properties": {
        "verai.aiVerifyPath": {
          "type": "string",
          "default": "verkeep-verify",
          "description": "Path to the verkeep-verify CLI executable (must be on PATH or absolute)."
        },
        "verai.tasksLimit": {
          "type": "number",
          "default": 15,
          "description": "How many recent sessions to list in the sidebar."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "test": "npm run compile && tsc -p tsconfig.contracts.json && node --test test/*.test.js",
    "watch": "tsc -watch -p ./",
    "package": "npm run compile && vsce package --no-dependencies",
    "accept": "npm run compile && node --test test/accept-parity.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^2.24.0",
    "typescript": "^5.3.0"
  }
}
