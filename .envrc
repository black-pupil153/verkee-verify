# direnv: 进入 verkeep-verify/ 目录自动把 venv 加入 PATH
# 首次使用: brew install direnv && echo 'eval "$(direnv hook zsh)"' >> ~/.zshrc
if [ -d venv ]; then
  export VIRTUAL_ENV="$PWD/venv"
  export PATH="$VIRTUAL_ENV/bin:$PATH"
fi
